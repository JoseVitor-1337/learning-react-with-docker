version: "3"

services:
  react-app:
    image: "react-image-dev"
    container_name: "react-app-dev"
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src:ro
    env_file:
      - ./.env
